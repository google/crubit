# Part of the Crubit project, under the Apache License v2.0 with LLVM
# Exceptions. See /LICENSE for license information.
# SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception

[package]
name = "ir"
edition = "2021"

[lib]
path = "../../ir.rs"

[dependencies]
flagset.workspace = true
proc-macro2.workspace = true
quote.workspace = true
serde.workspace = true
serde_json.workspace = true
arc_anyhow = { path = "../../../common/car<internal link>_anyhow" }
code_gen_utils = { path = "../../../common/car<internal link>_gen_utils" }
crubit_feature = { path = "../../../common/car<internal link>_feature" }
error_report = { path = "../../../common/car<internal link>_report" }
